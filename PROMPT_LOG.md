# PROMPT LOG — Лабораторная работа №12

---

## Задание 1 (средняя): Генерация CRUD-приложения

**Инструмент:** Claude (claude.ai)

**Промпт:**
```
Ты — senior Python разработчик. Используя FastAPI, создай CRUD-приложение для управления
анкетами репетиторов на платформе для репетиторов.

Модель репетитора содержит:
- id (int, автогенерация)
- full_name (str) — ФИО репетитора
- email (str) — Email
- subject (str) — преподаваемый предмет
- hourly_rate (float, >= 0) — стоимость занятия в час (руб.)
- experience_years (int, 0-60) — опыт преподавания (лет)
- education (str) — образование
- bio (str, опционально) — о себе
- is_active (bool, default=True) — активна ли анкета

Добавь эндпоинты:
- GET /tutors — список всех репетиторов
- POST /tutors — создать анкету
- GET /tutors/{id} — получить по ID
- PUT /tutors/{id} — обновить (частичное обновление)
- DELETE /tutors/{id} — удалить

Используй Pydantic v2 для валидации. Добавь обработку ошибок (404, 422).
Используй in-memory хранилище (dict). Покрой код type hints и напиши краткую документацию.
```

**Что сделано:**
1. Создан `task1/main.py` — FastAPI приложение с 5 эндпоинтами CRUD для сущности Tutor
2. Создан `task1/requirements.txt` — зависимости проекта


**Что исправлялось вручную:**
- Ничего. Код сгенерирован корректно с первого промпта.
- Единственная корректировка: убрал `pydantic[email]` из зависимостей (не используем EmailStr для простоты, оставили str)

### Итого
- Количество промптов: 1
- Время: ~15 мин

---

## Задание 2 (средняя): Генерация тестов

**Инструмент:** Claude (claude.ai)

**Промпт:**
```
Используя pytest и FastAPI TestClient, сгенерируй модульные тесты для CRUD-приложения
платформы репетиторов (main.py). Тесты должны покрывать:

1. CREATE: успешное создание, создание без опционального поля, невалидные данные
   (отрицательная цена, опыт > 60 лет, пустое имя, отсутствующее обязательное поле)
2. READ: получить всех (пустой список), получить всех (с данными), получить по ID,
   получить несуществующий ID (404)
3. UPDATE: частичное обновление, полное обновление, обновление несуществующего (404),
   невалидное значение
4. DELETE: успешное удаление, удаление несуществующего (404), проверка что удалён из списка
5. Граничные случаи: порядок ID, нулевая цена, нулевой опыт

Используй fixture autouse для очистки in-memory БД перед каждым тестом.
Целевое покрытие — не менее 70% ключевых маршрутов.
```

**Что сделано:**
1. Создан `task1/test_main.py` — 22 теста для всех маршрутов CRUD
2. Используется `autouse` fixture для изоляции тестов
3. Покрытие: CREATE (6 тестов), READ (4 теста), UPDATE (4 теста), DELETE (3 теста), граничные случаи (3 теста) + fixture (2)

**Что исправлялось вручную:**
- Ничего

### Итого
- Количество промптов: 1
- Время: ~10 мин

---

## Задание 3 (средняя): Рефакторинг «плохого» кода

**Инструмент:** Claude (claude.ai)

**Промпт:**
Ты — senior Python разработчик. Отрефактори этот код для платформы репетиторов.
Объясни каждое изменение.

[вставлен bad_code.py]

**Что сделано:**
1. Создан `task3/bad_code.py` — функция `calc()` с 5 антипаттернами: одна функция на 40 строк,
   магические числа (0.9, 0.06, 500...), имена `t/s/r/x`, дублирование в `get_tutors_list`/`get_top`, нет обработки ошибок
2. Создан `task3/refactored_code.py` — рефакторинг: декомпозиция на 3 функции,
   именованные константы, ValueError при невалидных данных, единый `_filter_active_tutors()`,
   `sorted()` вместо пузырьковой сортировки, полные type hints и docstrings

**Что исправлялось вручную:** ничего

### Итого
- Количество промптов: 1
- Время: ~10 мин

---

## Задание 4 (средняя): Генерация Docker-конфигурации

**Инструмент:** Claude (claude.ai)

**Промпт:**
Ты — senior Python разработчик. Создай Dockerfile и docker-compose.yml для FastAPI-приложения
платформы репетиторов. Dockerfile должен собирать приложение на python:3.12-slim,
docker-compose поднимает приложение и PostgreSQL 16. Учти: healthcheck для БД,
зависимость app от db, запуск миграций Alembic перед стартом приложения,
все секреты через переменные окружения. Добавь .env.example с описанием переменных.

**Что сделано:**
1. Создан `Dockerfile` — образ на python:3.12-slim, оптимизированные слои кеша,
   запуск `alembic upgrade head` перед стартом uvicorn
2. Создан `docker-compose.yml` — сервисы app и db (postgres:16-alpine),
   healthcheck, depends_on с condition: service_healthy, именованный volume
3. Создан `.env.example` — шаблон переменных окружения

**Что исправлялось вручную:** - Добавлен .dockerignore — исключает .env, __pycache__, тесты и .git из образа

### Итого
- Количество промптов: 1
- Время: ~10 мин

---

## Задание 5 (средняя): Объяснение сложного кода

**Инструмент:** Claude (claude.ai)

**Промпт:**
Объясни простым языком работу этой функции расчёта выплаты репетитору.
Разбери каждый шаг, объясни почему важен порядок элементов в списках,
что делает next() с генератором. Предложи улучшения.

[вставлен код calculate_payout из refactored_code.py]

**Что сделано:**
1. Создан `task5/explanation.md` — пошаговый разбор функции `calculate_payout`:
   механика скидки за опыт, бонус за рейтинг через `next()`, штраф за отмены,
   расчёт налога НПД 6%
2. Добавлены 4 предложения по улучшению: защита от отрицательной выплаты,
   логирование, TAX_RATE из env, dataclass вместо dict

**Что исправлялось вручную:** ничего

### Итого
- Количество промптов: 1
- Время: ~5 мин

---

## Задание 6 (средняя): Генерация документации

**Инструмент:** Claude (claude.ai)

**Промпт:**
Ты — senior Python разработчик. Изучи мой репозиторий https://github.com/BrakS268/Lab12-Chuev-Maxim
и создай полную документацию README.md для проекта «Платформа для репетиторов».
Документация должна включать: название и описание проекта, инструкцию по установке и запуску
(локальный + Docker с учётом docker-compose.yml и Alembic-миграций), описание всех API-эндпоинтов
с примерами curl-запросов и ответов, таблицу переменных окружения из .env.example, структуру репозитория.
Ориентируйся на реальное содержимое репо.

**Что сделано:**
1. Изменен README.md — полная документация проекта
2. Структура репо актуальна (task1-2, task3, task5, Dockerfile, docker-compose.yml)
3. Описаны оба способа запуска: локальный и через Docker Compose
4. Таблица переменных окружения из реального .env.example
5. Все 5 эндпоинтов с таблицей полей модели и curl-примерами
6. Раздел запуска тестов

**Что исправлялось вручную:** ничего

### Итого
- Количество промптов: 1
- Время: ~10 мин

---

## Задание 7 (средняя): Генерация миграций БД

**Инструмент:** Claude (claude.ai)

**Промпт:**
Ты — senior Python разработчик. Вот моя модель данных из main.py (Tutor с полями:
full_name, email, subject, hourly_rate, experience_years, education, bio, is_active).
БД — PostgreSQL. Сгенерируй миграции Alembic для создания и обновления схемы БД.
Учти индексы для часто запрашиваемых полей и ограничения на значения полей.

[вставлен код main.py]

**Что сделано:**
1. Создан `task7/database.py` — подключение SQLAlchemy, SESSION, get_db()
2. Создан `task7/models.py` — ORM-модель Tutor с индексами
3. Создан `task7/alembic.ini` — конфигурация Alembic, DATABASE_URL из env
4. Создан `task7/alembic/env.py` — окружение для online/offline режимов миграций
5. Создан `task7/alembic/versions/001_initial` — создание таблицы tutors,
   индексы по id, email (unique), full_name, subject, is_active
6. Создан `task7/alembic/versions/002_add_rating` — добавление полей rating и reviews_count
7. Создан `task7/alembic/versions/003_add_constraints` — CHECK-ограничения и
   составной индекс (subject, is_active)

**Что исправлялось вручную:** ничего

### Итого
- Количество промптов: 1
- Время: ~15 мин

---

## Задание 8 (средняя): Поиск уязвимостей в коде

**Инструмент:** Claude (claude.ai) в роли senior code reviewer

**Промпт:**
Ты senior python разработчик и security code reviewer.
Найди потенциальные уязвимости в коде FastAPI-приложения:
SQL-инъекции, XSS, незащищённые эндпоинты, отсутствие валидации входных данных,
отсутствие rate limiting, CORS, логирования.
Дай отчёт с severity-уровнями и конкретными рекомендациями по исправлению.

[вставлен код main.py — FastAPI Tutor Platform]

**Что сделано:**
1. Создан `task8/report.md` — отчёт с 8 уязвимостями, разбитыми по severity:
   - 🔴 Критические: отсутствие аутентификации
   - 🟠 Высокие: email без валидации (`str` вместо `EmailStr`), XSS в текстовых полях
   - 🟡 Средние: нет rate limiting, нет CORS, нет верхней границы у `hourly_rate`
   - 🟢 Низкие: нет логирования, race condition в in-memory БД
2. Создан `task8/main_secure.py` — исправленная версия с:
   - `EmailStr` для валидации email
   - `field_validator` с проверкой HTML-тегов (XSS)
   - `CORSMiddleware` с белым списком доменов
   - In-memory rate limiter (60 req/min per IP) через `Depends`
   - `logging` на каждый запрос (IP, действие, ID)
   - Верхняя граница `le=100_000` для `hourly_rate`

**Вывод:** Обнаружено 8 уязвимостей; 6 исправлены в коде, 2 описаны как архитектурные
(аутентификация и замена БД — требуют отдельных задач).

## Итого
- Количество промптов: 1
- Время: ~7 мин

---

## Задание 9 (средняя): Генерация SQL-запросов

**Инструмент:** Claude (claude.ai)

**Промпт:**
Ты senior SQL-разработчик. Предметная область — платформа для репетиторов.
Таблицы: tutors, students, lessons, reviews.
Сгенерируй 3 аналитических SQL-запроса:
1. Топ-5 репетиторов по количеству проведённых занятий за последние 30 дней
   с их средним рейтингом.
2. Выручка каждого репетитора по месяцам за текущий год.
3. Студенты, занимавшиеся более чем с 2 репетиторами, но не оставившие
   ни одного отзыва (churn risk).
Для каждого запроса объясни логику каждого ключевого элемента.

**Что сделано:**
1. Создан `task9/queries.sql` — три готовых SQL-запроса с комментариями:
   - Топ-5 репетиторов: JOIN + LEFT JOIN + скользящее окно 30 дней + двойная сортировка
   - Выручка по месяцам: DATE_TRUNC + деление на 60.0 (float) + EXTRACT(YEAR)
   - Churn risk студенты: NOT IN (subquery) + COUNT(DISTINCT) + HAVING
2. Создан `task9/explanation.md` — подробный разбор каждого запроса
   в виде таблиц с объяснением каждого элемента и бизнес-задачи.

**Вывод:** Все три запроса написаны под PostgreSQL; использованы оконные функции
DATE_TRUNC, EXTRACT, ROUND с ::NUMERIC-приведением. Отмечена альтернатива
NOT IN → NOT EXISTS для оптимизации при наличии NULL.

### Итого
- Количество промптов: 1
- Время: ~10 мин


## Задание 10 (лёгкая): Генерация регулярного выражения

**Инструмент:** Claude (claude.ai)

**Промпт:**
Ты senior Python-разработчик. Предметная область — платформа для репетиторов.
Сущности: репетиторы, студенты, занятия, отзывы.
Сгенерируй регулярное выражение для валидации ID занятия на платформе:
- формат: TUT-YYYY-XX (префикс, год 2000–2099, 2 заглавные буквы предмета)
- напиши Python-скрипт с набором валидных (мин. 6) и невалидных (мин. 10) примеров.
Для каждой части регулярного выражения объясни, что именно она проверяет
и почему такое ограничение важно для данной предметной области.

**Что сделано:**
1. Выбран формат ID занятия: `TUT-YYYY-XX`
   - `TUT` — фиксированный префикс платформы
   - `YYYY` — год занятия (диапазон 2000–2099)
   - `XX` — двухбуквенное обозначение предмета (заглавные латинские буквы)
2. Сгенерировано регулярное выражение: `^TUT-20\d{2}-[A-Z]{2}$`
   - `^` / `$` — якоря начала и конца строки (исключают пробелы и лишние символы)
   - `TUT-` — литеральный префикс
   - `20\d{2}` — год от 2000 до 2099 (`20` + любые 2 цифры)
   - `-[A-Z]{2}` — дефис и ровно 2 заглавные латинские буквы
3. Создан `task10/validate_session_id.py`:
   - функция `validate_session_id(session_id)` возвращает `bool`
   - 6 валидных примеров: `TUT-2024-MA`, `TUT-2025-EN`, `TUT-2000-PH` и др.
   - 11 невалидных примеров: короткий год, строчные буквы, нет дефисов и др.
   - все 17 тестов прошли с результатом «🎉 Все тесты прошли успешно!»

   ### Итого
- Количество промптов: 1
- Время: ~10 мин